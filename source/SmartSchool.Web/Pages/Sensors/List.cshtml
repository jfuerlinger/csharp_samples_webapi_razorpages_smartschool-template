@page
@model SmartSchool.Web.Pages.Sensors.ListModel
@{
    ViewData["Title"] = "List";
}

<h2>Last 20 Measurements for Sensor</h2>

<div class="row">
    @*<div class="col-md-4">
        <form name="formList" method="post" onchange="DoPostBack();">
            <div class="form-group">
                <label asp-for="SelectedSensorId" class="control-label"></label>
                <select asp-for="SelectedSensorId" class="form-control" asp-items="@Model.Sensors"></select>
            </div>
        </form>
    </div>*@
</div>

<table class="table">
    @*<thead>
        <tr>
            <th>
                Date
            </th>
            <th>
                Time
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Measurements[0].Value)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Measurements)
        {
            <tr>
                <td>
                    @item.Time.ToShortDateString()
                </td>
                <td>
                    @item.Time.ToShortTimeString()
                </td>
                <td>
                    @string.Format($"{item.Value} {Model.Sensor.Unit}")
                </td>
            </tr>
        }
    </tbody>*@
</table>

<div>
    <a asp-page="/Index">Back to List</a>
</div>

<script>
    function DoPostBack() {
        document.formList.submit();
    }
</script>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}


